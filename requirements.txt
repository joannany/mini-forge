# Core
pyyaml
pandas
streamlit

# Training / model generation (install in Colab or another GPU environment)
# unsloth
# transformers
# accelerate
# peft
# datasets

# Serving (when you have GPU access)
# vllm

# Judge / generation provider (pick one)
# openai          # works against a vLLM OpenAI-compatible endpoint too
# anthropic

# Optional production-grade PII scanning
# presidio-analyzer
